let React = require('react');

let Examples = (props) => {
    return (
        <div>
        <h3>Examples</h3>
        <p> Welcome to the Examples Page.</p>
        </div>
    )
};

module.exports = Examples;